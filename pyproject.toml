[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Memlike"
dynamic = ["version"]
requires-python = ">= 3.6"
authors = [
  {name = "Anon Software Foundation", email = "foundation@anon.com"},
]
